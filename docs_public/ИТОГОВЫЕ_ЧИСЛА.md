# 📊 Итоговые числа — единый источник правды (one-pager)

> **Назначение.** Один экран с финальными числами для текста работы, слайдов и витрин (демо/README).
> **Все таблицы/слайды/проза ссылаются ТОЛЬКО на этот файл** (и его первоисточник).
> **Первоисточник (не править руками):** `artifacts/ab_test_headline.json` (sealed-test, вскрыт 1 раз).
> **Дата вскрытия sealed-test:** 2026-06-07. **Числа ФИНАЛЬНЫЕ — настройки НЕ тюнить, sealed НЕ вскрывать повторно.**
> Система финала: **zero-shot MegaDescriptor-L-384 + `unroll_ribbon`** (заявленная лучшая, зафиксирована на dev ДО вскрытия).

---

## 1. Главная таблица (sealed-test, held-out: нетронутые test+open_test, kpi_core, identity-level)

| Срез | n | recall@1 | recall@5 | KPI (75/95) |
|---|---|---|---|---|
| **overall** | 112 | **0.250** [CI 0.179–0.338] | **0.446** [0.358–0.539] | ✗ не взят |
| **PW** (ребристый тритон) | 11 | **0.818** | **1.000** | ✅ **проходит** |
| **TK temporal** (тритон Карелина) | 101 | **0.188** | **0.386** | ✗ — стена данных |

- **gallery = 238** в роли gallery для метрик (кропнуто 242 gallery-кадра → 4 потеряны детектором / gallery-only).
- **Open-set** (known=test 112, new=open_test 21): **AUROC 0.446** (≈0.5 — фактически нет разделения).
  Порог (калиброван на dev+open_dev, НЕ на test) = 0.641; known-удержано 0.277; new-ложно-known 0.381 — иллюстрация слабости, не рабочая точка.

## 2. Тройка знаменателей overall (показывать ВСЕ, не один оптимистичный)

| Знаменатель | recall@1 | recall@5 | смысл |
|---|---|---|---|
| identity-covered (n=112 с кропом) | 0.250 | 0.446 | «headline» — фото, прошедшие детектор |
| **pipeline** (n=113 офиц. проб) | **0.248** | **0.442** | честный end-to-end (1 проба без кропа = промах) |
| closed-set re-ID (true-id в галерее) | 0.262 | 0.467 | «чистая» способность узнавать при наличии особи в базе |

## 3. Промежуточная оценка на dev (OOF, kpi_core, identity-level) — для сравнения СИСТЕМ (не финал)

| Система | recall@1 | recall@5 | PW @1 | TK temporal @1 | решение |
|---|---|---|---|---|---|
| эмбеддер (zero-shot Mega + ribbon) | 0.18 | **0.375** | ~0.9 | ~0.11 | baseline |
| матчер пятен (guided, покрыт тестами) | 0.094 | 0.141 | 0.82 | 0.026 | < эмбеддера |
| гибрид (эмбеддер top-K → матчер) | 0.18 | 0.375 | 0.91 | 0.11 | no-harm (не улучшает значимо) |

- **dev AUROC open-set ≈ 0.58** (на витринах писать сплитом: **«sealed 0.45 / dev 0.58»**).
- Источники dev-чисел: `artifacts/ab_embed_metrics.json`, `artifacts/ab_spots_metrics.json`,
  `artifacts/ab_hybrid_metrics.json` (генерируются локально, в репозиторий не входят).

## 4. Главный вывод (одна фраза для защиты)

**KPI (top-1 ≥75 % И top-5 ≥95 %) на `kpi_core` не достигается; узкое место — temporal-срез тритона Карелины (TK).**
Барьер — в **ДАННЫХ** (воспроизводимость узора пятен между сессиями; новых данных не предусмотрено), а не в коде:
тот же конвейер **берёт KPI на PW** (0.818/1.000), значит ограничение **видоспецифично**, а не артефакт метода.
Заработано строгой оценкой (cross-fit OOF, парная статистика, sealed-test 1 раз; корректность матчера подтверждена тестами `tests/test_constellation_*.py` и sensitivity-сравнением ransac@120..2000 vs guided).
Оговорки для устойчивости к вопросам комиссии — `docs_public/МЕТОДОЛОГИЯ_оговорки.md`.

## 5. Сравнения (держать качественными, не числовыми)
- **vs embedding-only подходы (DINOv2):** прямое числовое сравнение невозможно (другие данные/протокол, нет общего сплита).
  Дифференциатор — **интерпретируемый оверлей совпавших пятен**, которого embedding-only baseline структурно дать не может.
- **vs triton 2.0:** только методологически (кроп тела → кроп брюшка + ориентация + распрямление); обе версии меряны на нашем temporal-протоколе.
- **Планка 75/95** — внешняя калибровка по SoTA-foundation (MiewID на hard-GCN 73/93), другой вид/режим; целевой режим (temporal-дрейф TK) ТРУДНЕЕ калибровочного.
